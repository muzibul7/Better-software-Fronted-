# Frontend (React + Vite)

## Setup

```bash
cd frontend
npm install
```

## Run

```bash
npm run dev
```

Open the app at `http://localhost:5173`.

The dev server proxies `/api` to the FastAPI server at `http://localhost:8000`.

